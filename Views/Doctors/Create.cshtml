@{
  Layout = "_Layout";
}

@model Doctor_Office.Models.Doctor;

<h4>Add a Doctor</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br><span class="glyphicon glyphicon-plus" aria-hidden="true"></span><br><input class="btn" type="submit" value="Add a Doctor">
}