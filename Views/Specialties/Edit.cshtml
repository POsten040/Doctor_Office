@{
  Layout = "_Layout";
}

@model Doctor_Office.Models.Specialty;

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.SpecialtyId)
  <h3>Change Specialty:</h3>@Html.EditorFor(model => model.Name) <br/>
  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><br><input class="btn" type="submit" value="Save">
}