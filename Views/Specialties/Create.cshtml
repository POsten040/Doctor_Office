@{
  Layout = "_Layout";
}

@model Doctor_Office.Models.Specialty;

<h4>Add a Specialty</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br><span class="glyphicon glyphicon-plus" aria-hidden="true"></span><br><input class="btn" type="submit" value="Add a Specialty">
}